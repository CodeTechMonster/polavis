services:
  backend:
    build: ./backend
    ports:
      - "8787:8787"
    volumes:
      - ./data:/app/data
    env_file:
      - ./backend/.env
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
